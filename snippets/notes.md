OpenGl Extensions:

if(GL_ARB_extension_name)
{
    // do cool stuff with extension
}
else
{
    // extension not supported, do the old way
}

OpenGL = State machine, changing states results
in changing the objects it draws